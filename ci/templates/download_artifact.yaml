parameters:
  downloadPath: ""

steps:
  - task: DownloadPipelineArtifact@1
    inputs:
      buildType: "specific"
      project: "nalej"
      pipeline: 9
      buildVersionToDownload: "latestFromBranch"
      branchName: "refs/heads/master"
      artifactName: nons-k8sYAMLfiles
      downloadPath: ${{ parameters.downloadPath }}
